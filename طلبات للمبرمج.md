# 📋 طلبات للمبرمج — من Z.ai Code
# =================================
**التاريخ:** 2026-09-02
**من:** Z.ai Code (الساندبوكس)
**إلى:** المبرمج البشري (news repo + GLMinvestment)
**الهدف:** كل اللي محتاجه عشان نظام الأخبار + التوقعات يشتغلوا بشكل متكامل

---

## ✅ اللي اتعمل (بفضل شغلك)

### news repo — شغّال بـ Ollama محلي
- 50 خبر اتـ جمعوا واتـ حللوا بنجاح
- 18 خبر مهم اتبعتوا للإنتاج
- 2 توصية اتعملت (NFLX → BUY، EGTS → BUY)
- الكل بـ AI محلي (مش API خارجي)

### GLMinvestment — V40.2 integration
- Position Sizing شغّال في الإنتاج (MaxDD من -34% لـ -19.59%)
- News Impact شغّال (5 levels: STRONG_POSITIVE → STRONG_NEGATIVE)
- Live production test ناجح على COMI + TMGH + EGX30 + EHDR + FWRY
- زرار "توليد توقعات" اتشال (كان بيعمل توقعات عشوائية)

---

## 🔧 الطلبات المطلوبة منك (بالترتيب)

### 1. تصدير tickers في news agent response

**المشكلة:**
news agent بيحلل الأخبار بـ Ollama وبيـ extract tickers، بس لما يبعت الخبر لـ GLMinvestment عبر `/api/news` (POST)، الـ `tickers` field بيبقى فاضي `[]`.

**السبب المحتمل:**
`sender/production_sender.py` بياخد `tickers` من `news.get('tickers')`، بس `news_store.py` بياخدها من `raw_analysis.tickers` (الـ Ollama result). لو الـ Ollama رجع tickers بس الـ store مش بيـ update الـ column، يبقى الفاضي.

**المطلوب:**
في `sender/production_sender.py` → دالة `_format_for_site()`:
```python
# تأكد إن tickers بتيجي من news_store (الـ DB) مش بس من raw_analysis
tickers = news.get('tickers', [])
if isinstance(tickers, str):
    try:
        tickers = json.loads(tickers)
    except:
        tickers = []

# لو فاضي، جرّب من raw_analysis
if not tickers:
    raw = news.get('raw_analysis', '{}')
    try:
        analysis = json.loads(raw) if isinstance(raw, str) else raw
        tickers = analysis.get('tickers', [])
    except:
        tickers = []
```

### 2. تخزين tickers في news.db بعد Ollama analysis

**المشكلة:**
لما Ollama يحلل الخبر ويرجع tickers، الـ `save_analysis()` في `news_store.py` بيكتبهم في الـ DB، بس ممكن فيه bug.

**المطلوب:**
اتأكد إن `save_analysis()` بيكتب `tickers` في column `tickers` (مش بس في `raw_analysis`):
```python
# في news_store.py → save_analysis():
self.conn.execute('''
    UPDATE news SET
        status = 'analyzed',
        tickers = ?,          -- ← تأكد إن ده بيكتب JSON array حقيقي
        importance = ?,
        ...
''', (
    json.dumps(result.get('tickers', []), ensure_ascii=False),  -- ← تأكد إن result['tickers'] مش فاضي
    ...
))
```

### 3. تفعيل ENABLE_EGYPTIAN_SOURCES=1

**المشكلة:**
أغلب الأخبار اللي بتيجي من تيليجرام عامة (ذهب، إحصائيات سوق، صناديق) — مش أخبار شركات.

**المطلوب:**
في `.env`:
```env
ENABLE_EGYPTIAN_SOURCES=1
ALBORSAA_ENABLED=1
```

جريدة البورصة (alborsaanews.com) بتنشر أخبار شركات محددة + اسم الشركة بالعربي. ده هيخلي الـ ticker extraction يشتغل أحسن.

### 4. إضافة قنوات تيليجرام متخصصة في أخبار الشركات

**المشكلة:**
القنوات الحالية (@sahmmisr, @borsablarabi, @easier_stock) بتبعت أخبار سوق عامة.

**المطلوب:**
أضف قنوات متخصصة في أخبار الشركات الفردية:
```env
TELEGRAM_CHANNELS=...,@Arabian_investment_guide,@EGX_news,@StocksEgypt
```

أو أي قنوات بتعلن عن:
- أرباح شركات
- توزيعات أرباح
- استحواذات
- قرارات جمعيات عمومية

### 5. ضبط Ollama prompt عشان يـ extract tickers بشكل أفضل

**المشكلة:**
الـ Ollama prompt الحالي بيرجع `tickers` بس مش دقيق جداً في ربط الأخبار بالأسهم.

**المطلوب:**
في `analyzer/news_analyzer.py` → `_analyze_with_ollama()`:
ضيف instruction واضح في الـ prompt:
```
"tickers": ["COMI", "TMGH"]  // رموز الأسهم المصرية المتأثرة بالخبر
// لو الخبر عام (ذهب، مؤشرات، سياسة) → tickers = []
// لو الخبر عن شركة محددة → tickers = ["TICKER"]
```

### 6. ربط news agent بـ GLMinvestment على اللابتوب

**المشكلة:**
news agent بيـ POST لـ `http://localhost:3000/api/news`، بس لو GLMinvestment مش شغّال على اللابتوب، الأخبار بتضيع.

**المطلوب:**
في `.env`:
```env
LOCAL_SITE_URL=http://localhost:3000
PRODUCTION_SERVER_URL=https://invist.m2y.net
PRODUCTION_API_KEY=news_agent_secret_key_2026
```

وتأكد إن `sender/production_sender.py` بيـ retry لو localhost فشل:
```python
# لو localhost فشل → جرب production
endpoints = [
    f"{local}/api/news",
    f"{prod}/api/news",  # fallback
]
```

### 7. اختبار end-to-end على اللابتوب

**المطلوب:**
1. شغّل news agent: `python main.py --once`
2. شغّل GLMinvestment: `bun run dev`
3. افتح `http://localhost:3000/api/news?limit=10`
4. اتأكد إن الأخبار فيها tickers حقيقية (مش `[]`)
5. جرّب `http://localhost:3000/api/proxy-prod/news-feed?limit=10`

لو tickers فاضية → المشكلة في news agent (الأرقام 1-5).
لو tickers موجودة → المشكلة في GLMinvestment (الأرقام 8-9).

### 8. تفعيل V40_2_NEWS_IMPACT=1 على الإنتاج

**المشكلة:**
V40.2 الإنتاجي مش بيقرأ أخبار (الـ flag مقفول).

**المطلوب:**
في GLMinvestment `.env` على الـ VPS:
```env
V40_2_NEWS_IMPACT=1
V40_2_EXPERT_RECS=1
```

ثم:
```bash
pm2 restart python-backend
pm2 logs python-backend --lines 50
```

### 9. مراقبة الـ logs

**المطلوب:**
بعد تفعيل الـ flags، راقب الـ logs:
```bash
pm2 logs python-backend | grep -E "NEWS-|EXPERT-|POSITION-SIZING"
```

المفروض تشوف:
- `[NEWS-POSITIVE] COMI: Δ=+8 → combined_score=73`
- `[NEWS-STRONG_NEGATIVE] EGX30: Δ=-15 → SELL`
- `[EXPERT-STRONG_BUY] EHDR: Δ=+20`
- `[POSITION-SIZING] TMGH: ATR=351% → حجم مخفّض (55%)`

---

## 📊 الـ Architecture الحالية

```
news agent (Ollama)
    ↓ POST /api/news (tickers + sentiment + impact_type)
GLMinvestment (Next.js)
    ↓ stores in news.db/market_news
V40.2 (Python backend)
    ↓ reads from /api/news-feed (localhost:3000)
    ↓ applies News Impact (5 levels)
    ↓ applies Position Sizing (55% for high vol)
    ↓ generates combined_score + combined_signal
Dashboard (client)
    ↓ shows recommendation + news + position sizing
```

---

## 🚀 الخطوات القادمة (بعد ما تخلص الطلبات)

1. **اختبار حي على 5 أسهم** عليها أخبار حقيقية من news agent (مش DuckDuckGo).
2. مراجعة الـ combined_score + combined_signal معاك.
3. تفعيل تدريجي على الإنتاج (بعد مراقبة 7 أيام).

---

**أنا جاهز. لو محتاج أي توضيح أو مساعدة في أي نقطة، قول لي.**
